# Server Configuration
PORT=8080
ENV=development

# Database Configuration
DATABASE_URL=postgres://newsletter:password@localhost:5432/newsletter_db?sslmode=disable
DB_HOST=localhost
DB_PORT=5432
DB_USER=newsletter
DB_PASSWORD=password
DB_NAME=newsletter_db

# Redis Configuration
# For local development:
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_ADDR=localhost:6379

# For cloud Redis (Upstash example):
# REDIS_HOST=your-redis-host.upstash.io
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
# REDIS_ADDR=your-redis-host.upstash.io:6379

# SMTP Configuration (Brevo)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=your_brevo_email@example.com
SMTP_PASSWORD=your_brevo_smtp_key
SMTP_FROM_EMAIL=noreply@yourapp.com
SMTP_FROM_NAME=Newsletter App

# Logging
LOG_LEVEL=info

# Scheduler settings
SCHEDULER_INTERVAL=30s
SCHEDULER_BATCH_SIZE=100

# Asynq Redis settings
# For local development:
ASYNQ_REDIS_ADDR=localhost:6379
ASYNQ_REDIS_PASSWORD=
ASYNQ_REDIS_DB=0

# For cloud Redis (Upstash example):
# ASYNQ_REDIS_ADDR=your-redis-host.upstash.io:6379
# ASYNQ_REDIS_PASSWORD=your_redis_password
# ASYNQ_REDIS_DB=0
